<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.chyuan.ai.infrastructure.dao.IAdminUserDao">

    <resultMap id="McpAdminUserMap" type="cn.chyuan.ai.infrastructure.dao.po.McpAdminUserPO">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password_hash" property="passwordHash"/>
        <result column="role" property="role"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="queryByUsername" resultMap="McpAdminUserMap">
        SELECT id, username, password_hash, role, status, created_at, updated_at
        FROM mcp_admin_user
        WHERE username = #{username}
    </select>

    <select id="countAll" resultType="java.lang.Integer">
        SELECT count(*) FROM mcp_admin_user
    </select>

    <insert id="insert" parameterType="cn.chyuan.ai.infrastructure.dao.po.McpAdminUserPO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO mcp_admin_user (username, password_hash, role, status)
        VALUES (#{username}, #{passwordHash}, #{role}, #{status})
    </insert>

    <select id="countByRole" resultType="java.lang.Integer">
        SELECT count(*) FROM mcp_admin_user WHERE role = #{role}
    </select>

    <update id="updateRole">
        UPDATE mcp_admin_user SET role = #{role} WHERE username = #{username}
    </update>

</mapper>
